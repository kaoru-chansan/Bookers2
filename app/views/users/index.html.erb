<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">

      <%= render partial: 'from', locals: { user: current_user, book: @new_book } %>



              <th>name</th>
              <th></th>
            </tr>
        </table>

<h2>Users</h2>

<% @users.each do |user| %>
  <div>
    <p>image：<%= image_tag user.get_profile_image(100,100) %></p>
    <p>name：<%= user.name %></p>

    <%= link_to "Show", user_path(user.id) %>
  </div>
<% end %>